import { expect, test } from "bun:test";
import { useLiveEngineerStore } from "../src/stores/live-engineer";
test("bounds visible callout queue and tracks outbound controls", () => { const store = useLiveEngineerStore.getState(); store.dismiss(); const base = { type: "live-engineer-callout" as const, protocolVersion: 1 as const, family: "opponent-pace" as const, sessionId: "s", timelineEpoch: 1, sourceSequence: 1, priority: "normal" as const, createdSessionTimeMs: 0, expiresSessionTimeMs: 10, render: { renderingVersion: "opponent-pace-v1" as const, textKey: "live_engineer_opponent_fastest" as const, parameters: { relation: "fastest-in-class" as const, scope: "class" as const, playerLapNumber: 1, playerLapTimeMs: 60_000, benchmarkLapTimeMs: 61_000, deltaMs: -1_000, benchmarkKind: "session-best" as const }, voice: { catalogVersion: "v1", mode: "automatic" as const, segmentIds: [] } } }; for (let i = 0; i < 5; i++) store.receiveCallout({ ...base, deliveryId: `d${i}`, decisionId: `dec${i}`, candidateId: `c${i}` }); expect(useLiveEngineerStore.getState().history).toHaveLength(5); store.enqueueControl({ type: "live-engineer-voice", protocolVersion: 1, action: "ready", deliveryId: "d4" }); expect(store.takeOutbound()?.type).toBe("live-engineer-voice"); });
