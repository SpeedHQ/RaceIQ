import { expect, test } from "bun:test";
import { LiveEngineerRuntime, type LiveEngineerRuntimeCandidate } from "../../server/live-strategy/live-engineer-runtime";
const candidate = (id: string, priority: "high" | "normal" | "low" = "normal"): LiveEngineerRuntimeCandidate => ({ candidateId: id, actionKey: "opponent-pace-status", cooldownGroup: "opponent-pace", sourceFactIds: ["fact"], policyVersion: "opponent-pace-v1", renderParameters: { relation: "within-class-pace", scope: "class", playerLapNumber: 1, playerLapTimeMs: 60_100, benchmarkLapTimeMs: 60_000, deltaMs: 100, benchmarkKind: "session-best" }, sessionId: "s", timelineEpoch: 1, sourceSequence: Number(id), priority, createdSessionTimeMs: 0, expiresSessionTimeMs: 10_000 });
test("bounds queue and selects priority order", () => { let now = 0; const selected: string[] = []; const runtime = new LiveEngineerRuntime({ now: () => now, maxQueue: 2, onSelected: (c) => selected.push(c.candidateId) }); runtime.reset("s", 1); runtime.submit(candidate("1", "low")); runtime.submit(candidate("2", "high")); runtime.submit(candidate("3", "normal")); runtime.selectNext(); expect(selected).toEqual(["2"]); expect(runtime.pendingCount).toBe(1); now = 11_000; expect(runtime.selectNext()).toBeNull(); });
