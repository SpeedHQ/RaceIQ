name: Playwright E2E (compiled PR gate)

on:
  workflow_call:

jobs:
  playwright:
    uses: ./.github/workflows/playwright.yml
    with:
      server-mode: compiled
      server-set: all
      runs-on: '["whitesmith-windows-x64", "10VCPU", "10G"]'
      dist-artifact: raceiq-dist-windows
      artifact-name: playwright-compiled
