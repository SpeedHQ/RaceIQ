pre-commit:
  parallel: true
  jobs:
    - name: lint
      root: "client/"
      glob: "client/src/**/*.{ts,tsx}"
      run: bunx eslint {staged_files}

    - name: typecheck
      glob: "server/**/*.ts"
      run: bunx tsc --noEmit --skipLibCheck
