{
  "name": "forza-telemetry",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run dev:server & bun run dev:client",
    "dev:server": "bun --watch run server/index.ts",
    "dev:client": "cd client && portless run --name forza bun vite",
    "build": "cd client && bun install && bun vite build && cd .. && bun scripts/bundle-client.ts && bun scripts/bundle-shared-data.ts && NODE_ENV=production bun build --compile server/index.ts --outfile dist/forza-telemetry",
    "start": "NODE_ENV=production ./dist/forza-telemetry",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "test": "bun test"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "better-sqlite3": "^12.8.0",
    "drizzle-kit": "^0.30.0",
    "portless": "^0.7.1"
  }
}
